# Screenshots

Drop PNGs here and reference them from MDX as `/screenshots/<name>.png` via the
`<Screenshot src="..." />` component. Any `<Screenshot>` without a `src` renders
a labelled placeholder, so the outstanding shots are visible in the built site.
